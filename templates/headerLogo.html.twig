<div class="row header">
    {% if route is defined %}
        <a class="col-3 text-center" href="{{ route }}">
            <i class="fa fa-chevron-left"></i>
        </a>

    {% else %}
        <a class="col-3 text-center" href="{{ path('profile_index') }}">
            <img src="{{ asset('build/images/logo-wastity_blanc.png') }}" class=" wastity_blanc">
        </a>
    {% endif %}
    <h1 class="col text-center">{{ type }}</h1>
    <a class="col-3 text-center" href="/logout">
        <i class="fas fa-unlock"></i>
        <span class="d-none d-sm-inline-block">Déconnexion</span>
    </a>
</div>
