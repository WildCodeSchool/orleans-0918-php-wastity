<form method="post" action="{{ path('foodhero_delete', {'id': FoodHero.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ FoodHero.id) }}">
    <button class="btn">Delete</button>
</form>
