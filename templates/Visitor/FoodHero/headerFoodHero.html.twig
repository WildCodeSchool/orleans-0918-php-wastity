{% block header %}
    <div class="row header">
        <a class="col-3 text-center" href="{{ path('profile_index') }}"><img src="{{ asset('build/images/logo-wastity_blanc.png') }}" class="img-fluid wastity_blanc"></a>
        {% if foodhero is defined %}
            <h1 class="col text-center">Food Hero</h1>
        {% else %}
            <h1 class="col text-center">Inscription</h1>
        {% endif %}
        <a class="col-3 text-center" href="/logout"><i class="fas fa-unlock"></i><p class="d-none d-md-block">Déconnexion</p></a>
    </div>
{% endblock %}