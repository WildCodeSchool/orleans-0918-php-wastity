{% extends 'Visitor/Association/layoutAssociation.html.twig' %}

{% block header %}
    {% include 'headerLogo.html.twig' with {'type': 'Association', 'route': path('association_show', {'id':association.id}) } %}
{% endblock %}

    {% block body %}
        <main class="container">
            <div class="row justify-content-center text-center">
                <div>
                    <p class="titlePage text-center text-success mt-3 mb-2"><strong>Modification des horaires</strong></p>
                </div>
                <div class="col-12 text-center text-success mt-3 mb-3">
                    <i class="far fa-calendar-alt fa-4x"></i>
                </div>
            </div>
            <div class="row">
                <div class="row text-center ">
                    {{ include('Visitor/Association/_formSchedule.html.twig', {'button_label': 'Enregistrer les horaires'}) }}
                </div>
            </div>
        </main>
    {% endblock %}
{% block footer %}{% endblock %}