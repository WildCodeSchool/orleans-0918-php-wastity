{% extends 'Visitor/Association/layoutAssociation.html.twig' %}

{% block title %}Association{% endblock %}

{% block body %}
    <h1>Association</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ association.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ association.name }}</td>
        </tr>
        <tr>
            <th>Adress</th>
            <td>{{ association.adress }}</td>
        </tr>
        <tr>
            <th>PostalCode</th>
            <td>{{ association.postalCode }}</td>
        </tr>
        <tr>
            <th>Town</th>
            <td>{{ association.town }}</td>
        </tr>
        <tr>
            <th>Phone</th>
            <td>{{ association.phone }}</td>
        </tr>
        <tr>
            <th>Mail</th>
            <td>{{ association.mail }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('association_index') }}">back to list</a>

    <a href="{{ path('association_edit', {'id': association.id}) }}">edit</a>

    {{ include('Visitor/Association/_delete_form.html.twig') }}
{% endblock %}
