<nav class="navbar fixed-bottom">
    <div class="row justify-content-center text-center">
        <div class="col-3">
            <a class="navbar-brand ml-2" href="{{ path('association_list_offers', {'id' : association.id}) }}">
                <i class="fas fa-clipboard-list fa-2x {% if (path('association_list_offers', {'id' : association.id})) == app.request.requestUri %} associationActive {% endif %}"></i>
                <p>Offres</p>
            </a>
        </div>
        <div class="col-3">
            <a class="navbar-brand" href="{{ path('association_record', {'id' : association.id}) }}">
                <i class="fas fa-history fa-2x  {% if (path('association_record', {'id' : association.id})) == app.request.requestUri %} associationActive {% endif %}"></i>
                <p>Historique</p>
            </a>
        </div>
        <div class="col-3">
            <a class="navbar-brand ml-3" href="{{ path('association_show_statistics', {'id' : association.id}) }}">
                <i class="fas fa-chart-line fa-2x  {% if (path('association_show_statistics', {'id' : association.id})) == app.request.requestUri %} associationActive {% endif %}"></i>
                <p>Stats</p>
            </a>
        </div>
        <div class="col-3">
            <a class="navbar-brand" href="{{ path('association_show', {'id' : association.id}) }}">
                <i class="fas fa-user-tie fa-2x  {% if (path('association_show', {'id' : association.id})) == app.request.requestUri %} associationActive {% endif %}"></i>
                <p>Admin</p>
            </a>
        </div>
    </div>
</nav>
