{% extends 'base.html.twig' %}

{% block title %}Edit Offer{% endblock %}
{% block stylesheets %}{{ encore_entry_link_tags('css/appCompany') }}{% endblock %}

{% block body %}
    <div class="row header">
        <a class="col-1" href="{{ path('profile_index') }}"><i class="fas fa-chevron-left"></i></a>
        <h1 class="col text-center">{{ offer.company.name }}</h1>
    </div>
<div class="container mb-5 pb-5">
    <div class="row justify-content-center text-center">
        <div>
            <p class="fa-2x text-center text-info mt-3 mb-2"><strong>Modifier mon offre</strong></p>
        </div>
    </div>
    {{ include('Visitor/Offer/_form.html.twig', {'button_label': 'Modifier'}) }}
</div>
{% endblock %}
