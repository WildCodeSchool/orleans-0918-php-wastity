<nav class="navbar fixed-bottom">
    <div class="row justify-content-center text-center">
        <div class="col-3">
            <a class="navbar-brand ml-2" href="{{ path('company_show_offers', {'id' : company.id}) }}">
                <i class="fas fa-clipboard-list fa-2x {% if (path('company_show_offers', {'id' : company.id})) == app.request.requestUri %} companyActive {% endif %}"></i>
                <p>Offres</p>
            </a>
        </div>
        <div class="col-3">
            <a class="navbar-brand " href="{{ path('company_record', {'id' : company.id}) }}">
                <i class="fas fa-history fa-2x {% if (path('company_record', {'id' : company.id})) == app.request.requestUri %} companyActive {% endif %}"></i>
                <p>Historique</p>
            </a>
        </div>
        <div class="col-3">
            <a class="navbar-brand ml-3 " href="{{ path('company_show_statistics', {'id' : company.id}) }}">
                <i class="fas fa-chart-line fa-2x {% if (path('company_show_statistics', {'id' : company.id})) == app.request.requestUri %} companyActive {% endif %}"></i>
                <p>Stats</p>
            </a>
        </div>
        <div class="col-3">
            <a class="navbar-brand" href="{{ path('company_show', {'id' : company.id}) }}">
                <i class="fas fa-user-tie fa-2x {% if (path('company_show', {'id' : company.id})) == app.request.requestUri %} companyActive {% endif %}"></i>
                <p>Admin</p>
            </a>
        </div>
    </div>
</nav>