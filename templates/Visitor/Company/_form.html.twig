<div class="row">
    <div class="col text-center mt-1">
        {{ form_start(form) }}
        <div class="form-group">
            {{ form_widget(form) }}
        </div>
        <button class="btn btn-info">{{ button_label|default('Création') }}</button>
        {{ form_end(form) }}
    </div>
</div>
